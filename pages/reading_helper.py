## This should be a webb site that reads the text from diffèrent authors and then shows, each of them will have a read button
## if that is pressed, then we will log it in a database and then we will show the next one
